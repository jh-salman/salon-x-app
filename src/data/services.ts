/**
 * Services: types and mock. All types live in ./types.ts; mock in ./mockData.ts.
 */

export type { ServiceOption, PriceType } from './types';
export {
  MOCK_SERVICES,
  SERVICE_CATEGORIES,
  CALENDAR_COLORS,
} from './mockData';
